# ────────────────────────────────────────────────────────────────
# 1.7 Основные элементы графика.
# ────────────────────────────────────────────────────────────────
# Рассмотрим основные термины и понятия, касающиеся изображения
# графика, с которыми вам необходимо будет познакомиться, для того,
# чтобы в дальнейшем у вас не было трудностей при изучении
# представленных здесь уроков и документации по библиотеке Matplotlib.

# Корневым элементом при построении графиков в системе Matplotlib
# является Фигура (Figure). Все, что нарисовано на рисунке выше
# является элементами фигуры. Рассмотрим ее составляющие более
# подробно.

# График.
#   На рисунке представлены два графика - линейный и точечный.
#   Matplotlib предоставляет огромное количество различных настроек,
#   которые можно использовать для того, чтобы придать графику
#   требуемый вид: задать цвет, толщину, тип, стиль линии и многое
#   другое, все это мы рассмотрим в ближайших уроках.

# Оси.
#   Вторым, после непосредственно самого графика, по важности
#   элементом фигуры являются оси. Для каждой оси можно задать
#   метку (подпись), основные (major) и дополнительные (minor)
#   элементы шкалы, их подписи, размер, толщину и диапазоны.

# Сетка и легенда.
#   Сетка и легенда являются элементами фигуры, которые
#   значительно повышают информативность графика. Сетка может
#   быть основной (major) и дополнительной (minor). Каждому типу
#   сетки можно задавать цвет, толщину линии и тип. Для отображения
#   сетки и легенды используются соответствующие команды.

import numpy as np
import matplotlib.pyplot as plt
from matplotlib.ticker import (MultipleLocator, FormatStrFormatter, AutoMinorLocator)

# 1. Подготовка данных
# Создаём массив равномерно распределённых значений по оси X
x = np.linspace(0, 10, 10)          # 10 точек от 0 до 10
y1 = 4 * x                          # линейная функция
y2 = [i**2 for i in x]              # квадратичная функция (можно было написать y2 = x**2)

# 2. Создание главной фигуры и системы координат (осей)
# fig — это "лист бумаги" (вся область рисунка)
# ax  — это область внутри фигуры, где будет график (axes = система координат)
fig, ax = plt.subplots(figsize=(8, 6))   # размер фигуры в дюймах (ширина 8, высота 6)

# 3. Настройка заголовка и подписей осей
ax.set_title('Графики зависимостей: y1=4*x, y2=x^2', fontsize=16)
ax.set_xlabel('x', fontsize=14)
ax.set_ylabel('y1, y2', fontsize=14)

# 4. Добавление сетки (основной и вспомогательной)
ax.grid(which='major', linewidth=1.2)                # основная сетка — толстая
ax.grid(which='minor', linestyle='--', color='gray', linewidth=0.5)  # вспомогательная — тонкая пунктирная

# 5. Построение самих графиков
ax.scatter(x, y1, c='red', label='y1 = 4*x')         # точечный график (маркеры)
ax.plot(x, y2, label='y2 = x^2')                     # линейный график (линия)

# 6. Добавление легенды (обозначение кривых)
ax.legend()

# 7. Настройка делений (тиков) на осях
# Включаем автоматическое добавление мелких делений (minor ticks)
ax.xaxis.set_minor_locator(AutoMinorLocator())
ax.yaxis.set_minor_locator(AutoMinorLocator())

# 8. Настройка внешнего вида делений (длина и толщина черточек)
ax.tick_params(which='major', length=10, width=2)    # основные деления — длинные и толстые
ax.tick_params(which='minor', length=5,  width=1)    # мелкие деления — короткие и тонкие

# 9. Отображение всего рисунка на экране
plt.show()
