[pytest]
minversion = 7.0
addopts = -q --tb=short --strict-markers
testpaths = Medium/tests
python_files = test_*.py
python_functions = test_*
filterwarnings =
    ignore::DeprecationWarning

# Для удобства в IDE
pythonpath = .