[pytest]
minversion = 8.0
addopts = -q --tb=short --strict-markers --cov=. --cov-report=term-missing -ra
testpaths = 
    Medium/test
    Well-done/test
python_files = test_*.py
python_functions = test_*
filterwarnings =
    ignore::DeprecationWarning

pythonpath = .